/**
 * [BACKUP OBSOLÈTE] Ce fichier n'est plus utilisé.
 * Voir js/crypto-keys.js (Secure Key Manager v2.0) pour le système actuel.
 * Les clés ont été retirées de ce fichier pour raison de sécurité.
 */

window.decodeKey = function() { return null; };
window.encodeKey = function() { return null; };